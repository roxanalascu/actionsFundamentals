name: Greet
author: Roxana
description: Greet and show current time

inputs:
  who-to-greet:
    description: Who to greet
    default: world
outputs:
  time:
    description: The current time

runs:
  using: docker
  image: Dockerfile
  args:
    - ${{ inputs.who-to-greet }}
  
